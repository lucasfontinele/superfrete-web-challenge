import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.superfrete.web.challenge',
  appName: 'superfrete-web-challenge',
  webDir: 'dist'
};

export default config;
